
<%= simple_form_for(current_status, html:{class: "form-horizontal" }) do |f| %>

    <% if current_status.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(current_status.errors.count, "error") %> prohibited this status from being saved:</h2>

          <ul>
            <% current_status.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

      <%= f.input :content, label: false, input_html: {placeholder: "What's on your mind?" } %>

      <div class="form-actions">
        <%= f.button :submit, "Post" %>
      </div>

<% end %>
