<%= simple_form_for(current_comment, html:{class: "form-horizontal" }) do |f| %>
    <% if current_comment.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(current_comment.errors.count, "error") %> prohibited this status from being saved:</h2>

          <ul>
            <% current_comment.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>
      <%= f.hidden_field :commentable_id %>
      <%= f.hidden_field :commentable_type %>
      <%= f.input :content, label: false, input_html: {placeholder: placeholder } %>

      <div class="form-actions">
        <%= f.button :submit, "Post" %>
      </div>
<% end %>